const daughters = ['Uadlsad', 'Cibalena'];
const sons = ['Uodsad', 'Uesclei'];
const all = daughters.concat(sons, 'Fulano');

console.log(all, sons, daughters);
console.log([].concat([1, 2], [3, 4], 5, [[6, 7]]));